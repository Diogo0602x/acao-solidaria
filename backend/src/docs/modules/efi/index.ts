import { authenticateToken } from '@docs/modules/efi/authenticate-token'
import { createImmediateCharge } from '@docs/modules/efi/create-immediate-charge'

const EfiRoutes = {
  authenticateToken,
  createImmediateCharge,
}

export { EfiRoutes }
